package start;
/*
import controllers.BookController;
import controllers.CartController;
import controllers.PaymentController;
import controllers.UserController;
import controllers.interfaces.IBookController;
import controllers.interfaces.ICartController;
import data.PostgresDB;
import data.interfaces.IDB;
import RoleManagement.Role;
import repositories.BookRepository;
import repositories.CartRepository;
import repositories.PaymentRepository;
import repositories.UserRepository;
import repositories.interfaces.IBookRepository;
import repositories.interfaces.ICartRepository;
import repositories.interfaces.IPaymentRepository;
import repositories.interfaces.IUserRepository;


public class Start_User extends Start{
    Role role = Role.USER;

    boolean doCart = true;
    boolean createUser = true;
    boolean getUserById = false;
    boolean getUserByName = false;
    boolean getUserByEmail = false;
    boolean loginMenu = true;

    public Start_User() {
        super();
    }

    public IUserRepository userRepo = new UserRepository(db);
    public UserController userController = new UserController(userRepo);

    public IBookRepository bookRepo = new BookRepository(db);
    public IBookController bookController = new BookController(bookRepo);

    public ICartRepository cartRepo = new CartRepository(db);
    public ICartController cartController = new CartController(cartRepo);

    public IPaymentRepository paymentRepo = new PaymentRepository(db);
    public PaymentController paymentController = new PaymentController(paymentRepo);


    public MyApplication app = new MyApplication(userController);
    public BookApplication bookApp = new BookApplication(bookController);
    public CartApplication cartApplication = new CartApplication(cartController);
    public PaymentApplication paymentApp = new PaymentApplication(paymentController);
}*/
